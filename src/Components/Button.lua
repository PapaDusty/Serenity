-- Button component for Serenity UI
return function(Title, Parent)
    local Serenity = require(script.Parent.Parent)
    
    local Button = {
        Type = "Button",
        Title = Title
    }

    -- Button implementation would go here
    -- This ensures the component exists
    
    return Button
end
